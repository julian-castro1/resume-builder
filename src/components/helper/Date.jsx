import { useState } from 'react'
import PropTypes from 'prop-types'

Date.propTypes = {
    name: PropTypes.string.isRequired,
}

function Date({name}){

    
    return (
         <>  </>
    )
}

export default Date