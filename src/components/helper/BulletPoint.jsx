import { useState } from 'react'
import PropTypes from 'prop-types'

BulletPoint.propTypes = {
    name: PropTypes.string.isRequired,
}

function BulletPoint({name}){

    
    return (
         <>  </>
    )
}

export default BulletPoint